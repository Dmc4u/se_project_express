// Define constants for HTTP status codes
const BAD_REQUEST = 400;
const NOT_FOUND = 404;
const DEFAULT = 500;

// Export the constants
module.exports = {
    BAD_REQUEST,
    NOT_FOUND,
    DEFAULT,
};
